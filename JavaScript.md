# JavaScript
- [公式](https://developer.mozilla.org/ja/docs/Web/JavaScript)
- [Wiki](https://en.wikipedia.org/wiki/JavaScript)

wiki(原文)
```
JavaScript (/ˈdʒɑːvəˌskrɪpt/),[7] often abbreviated as JS, is a high-level, interpreted programming language. It is a language which is also characterized as dynamic, weakly typed, prototype-based and multi-paradigm.

Alongside HTML and CSS, JavaScript is one of the three core technologies of the World Wide Web.[8] JavaScript enables interactive web pages and thus is an essential part of web applications. The vast majority of websites use it,[9] and all major web browsers have a dedicated JavaScript engine to execute it.

As a multi-paradigm language, JavaScript supports event-driven, functional, and imperative (including object-oriented and prototype-based) programming styles. It has an API for working with text, arrays, dates, regular expressions, and basic manipulation of the DOM, but the language itself does not include any I/O, such as networking, storage, or graphics facilities, relying for these upon the host environment in which it is embedded.

Initially only implemented client-side in web browsers, JavaScript engines are now embedded in many other types of host software, including server-side in web servers and databases, and in non-web programs such as word processors and PDF software, and in runtime environments that make JavaScript available for writing mobile and desktop applications, including desktop widgets.

Although there are strong outward similarities between JavaScript and Java, including language name, syntax, and respective standard libraries, the two languages are distinct and differ greatly in design; JavaScript was influenced by programming languages such as Self and Scheme. [10]
```
wiki(訳)
```
JavaScript（/dʒɑːvəˌskrɪpt/）[7]は、しばしばJSと略され、高水準の解釈されたプログラミング言語です。これは、動的で弱く型付けされたプロトタイプベースのマルチパラダイムとしても特徴付けられる言語です。

HTMLとCSSに加えて、JavaScriptはWorld Wide Webの3つのコアテクノロジーの1つです。[8] JavaScriptはインタラクティブなWebページを可能にするため、Webアプリケーションの不可欠な部分です。大多数のWebサイトで使用されています。[9]、すべての主要なWebブラウザには専用のJavaScriptエンジンがあります。

マルチパラダイム言語として、JavaScriptはイベント駆動、機能、および命令型（オブジェクト指向およびプロトタイプベースのプログラミングスタイルを含む）をサポートしています。テキスト、配列、日付、正規表現、DOMの基本操作を扱うためのAPIを備えていますが、言語自体にはネットワーキング、ストレージ、グラフィックス機能などのI / Oは含まれていません。埋め込まれたホスト環境

JavaScriptエンジンは、Webブラウザで初めて実装されたクライアント側のみで、Webサーバーやデータベースのサーバー側、ワードプロセッサやPDFソフトウェアなどの非Webプログラム、実行時などの他の多くの種類のホストソフトウェアに組み込まれていますデスクトップウィジェットを含むモバイルアプリケーションやデスクトップアプリケーションの作成にJavaScriptを利用できる環境

言語名、構文、それぞれの標準ライブラリを含む、JavaScriptとJavaの間には強い外見上の類似点がありますが、2つの言語は区別され、デザインが大きく異なります。 JavaScriptは、SelfやSchemeなどのプログラミング言語の影響を受けました。 [10]
```
- 動的で弱く片付けされたプロトタイプベースのマルチパラダイム言語
- HTMLとCSSに加えて、JavaScriptはWorld Wide Webの3つのコアテクノロジーの1つ
- すべての主要なWebブラウザには専用のJavaScriptエンジンがある
- マルチパラダイム言語として、イベント駆動、functional、オブジェクト指向、プロトタイプベースのプログラミングスタイルをサポート